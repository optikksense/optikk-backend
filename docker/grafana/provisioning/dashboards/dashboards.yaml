apiVersion: 1

providers:
  - name: optikk-backend
    folder: Optikk
    type: file
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
